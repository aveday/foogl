#pragma once

#include <string>
#include <vector>
#include <algorithm>
#include <list>
#include <math.h>

#include "Perlin.h"

#include "Mesh.h"
#include "Util.h"

//std::vector<Vertex>vloadModel(const std::string modelName);

class Assets
{
    public:
        static int Triangles();
        static int Terrain();
};
