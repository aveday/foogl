#pragma once

#include <string>
#include <vector>
#include <math.h>

#include "Mesh.h"
#include "Util.h"

//std::vector<Vertex>vloadModel(const std::string modelName);

class Assets
{
    public:
        static int Triangles();
        static int Terrain();
};
